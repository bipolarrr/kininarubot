services:
  bot:
    build:
      context: ./app
    image: kininarubot:latest
    network_mode: host
    restart: unless-stopped
    env_file:
      - .env
    environment:
      KININARUBOT_DATA_DIR: /data
    volumes:
      - ./data:/data
